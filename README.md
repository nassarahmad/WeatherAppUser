# WeatherAppUser

### npm install -y
### npm install 
### npm i  
    axios,bcrypt,
    body-parser,
    dotenv,
    express,
    jsonwebtoken,
    mysql2,
    pg

    PORT=3000
    DB_HOST=localhost
    DB_USER=????
    DB_PASSWORD=??????
    DB_NAME=weatherappuser
    JWT_SECRET= ??????????
    OPENWEATHER_API_KEY=your key
